Spam Message Detection using kNN

This project detects whether a message is spam or not using
machine learning.

The system uses TF-IDF vectorization and k-Nearest Neighbour
classifier with cosine distance.

Technologies:
Python, Flask, Scikit-learn

Steps to run:
1. pip install -r requirements.txt
2. python model/train_model.py
3. python app.py
